//setTimeout() = func in js that allows you to schedule
//               the execution of a func after an amount of time (miliseconds)
//              Times are approximates (varies based on the workload of the JavaScript)

//              setTimeout(callback, delay);


function sayHello(){
    
}
